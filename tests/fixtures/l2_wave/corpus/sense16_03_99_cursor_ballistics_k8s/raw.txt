The user wants to find two things:
1. The module calculating ballistic cursor trajectory for real-time sync with Kubernetes on user blink.
2. Where that physics is persisted in the quantum vector index of the workspace.

Work 1 (T1) already investigated the first part ("dónde se calcula la trayectoria balística...") and concluded "no hay objeto" (no object found). The code is a local text editor, no K8s sync, no ballistic physics.

The second part of the user's request is: "y dónde se persiste esa física en el índice vectorial cuántico del workspace".
Since the first part (the physics itself) doesn't exist, the second part (persisting that physics) likely doesn't exist either.
However, I should verify if there is *any* persistence of cursor state or similar in a "vector index" or similar structure, just to be thorough, or I can close the whole claim based on the first part's failure.

The instructions say: "Si el pack no nombra el mecanismo (ningún nombre en leído/visto), puedes cerrar sin hijo (no hay objeto)."
Also: "Si la última caza cerró: el siguiente tiro es un port
---- content ----
{"action":"control_v1","do":"cerrar","why":"el cálculo de trayectoria balística no existe; sin física, no hay persistencia en índice vectorial"}