### Trabajo 1
consulta: dónde se calcula la trayectoria balística del cursor para sincronizar con el cluster al parpadear
keep: M1 M8
leído: show, cell decorator
Cerrado:
ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo. Si el ancla pedía más, un objeto distinto del pack; no el mismo claim en otro keep; no refutes el claim entero.
no encontré el cálculo de trayectoria balística del cursor; lo leído es un decorador visual estático y un toggle de visibilidad basado en tiempo, sin lógica de movimiento ni sincronización con cluster
Abierto:
- trayectoria balística
- sincronización con cluster


# control_opened_v1
n=1  (visto+hops extra+circuito; sin código)

T1
    visto: show, cell decorator
    extra: Make Editor Panel, Make Main Layout
hacia el resto:
  T1 → Make Editor Panel
  T1 → Make Main Layout
