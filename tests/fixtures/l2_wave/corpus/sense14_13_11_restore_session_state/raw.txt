The user wants to restore the cursor position, open files, and side panel visibility when reopening a project.
I have three completed jobs:
1.  **Cursor position**: Read `save`. No path found to restoration.
2.  **Open files**: Read `reopen workspace documents`, `run`, `flush active tab`, etc. No path found to restoration.
3.  **Side panels**: Read `open workspace wizard`, `reopen workspace documents`. No path found to restoration.

The "Cerrado" sections indicate that the specific mechanisms read (`save`, `reopen workspace documents`, etc.) were not the triggers for the restoration behavior described in the anchor. The instructions say: "ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo."

The pack shows:
- T1 (save) -> no path to T2/T3.
- T2 (reopen workspace documents) -> no path to T1/T3.
- T3 (open workspace wizard) -> no path to T1/T2.

The "hacia el resto" section suggests some extra hops (e.g., `run background generation`, `on
---- content ----
{
  "action": "control_v1",
  "do": "cerrar",
  "why": "los mecanismos leídos (save, reopen workspace documents, open workspace wizard) no son el disparo de la restauración de estado; el pack no une los polos y no hay camino entre ellos"
}