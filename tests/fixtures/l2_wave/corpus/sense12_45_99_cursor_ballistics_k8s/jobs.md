### Trabajo 1
consulta: dónde se calcula la trayectoria de los cursores del editor para sincronizarlos en tiempo real
keep: M1
leído: multi cursor active, exit multi cursor mode
Cerrado:
ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo. Si el ancla pedía más, un objeto distinto del pack; no el mismo claim en otro keep; no refutes el claim entero.
no encontré cálculo de trayectoria ni sincronización en tiempo real; lo leído son funciones de estado booleano multi cursor active y colapso de cursores exit multi cursor mode sin lógica de movimiento, interpolación o red
Abierto:
- handle editor keys
- cursor movement logic
- cursor sync engine
- trajectory calculator


# control_opened_v1
n=1  (visto+hops extra+circuito; sin código)

T1
    visto: multi cursor active, exit multi cursor mode
    extra: make breadcrumb bar, handle editor keys
hacia el resto:
  T1 → make breadcrumb bar
  T1 → handle editor keys
