### Trabajo 1
consulta: dónde se extrae la línea de error del texto de la consola inferior
keep: M7
leído: scan shell output for linker errors, parse pty filter tokens
Cerrado:
leído: scan shell output for linker errors, parse pty filter tokens
Abierto:
(nada; no copiar ids del pack)

### Trabajo 2
consulta: dónde se dibuja la línea roja en el margen del editor
keep: M1
leído: scan shell output for linker errors, parse pty filter tokens
Cerrado:
leído: scan shell output for linker errors, parse pty filter tokens
Abierto:
(nada; no copiar ids del pack)


# control_opened_v1
n=2  (visto+hops extra+circuito; sin código)

T1
    visto: scan shell output for linker errors, parse pty filter tokens
    extra: tick binary symbols panel, has value

T2
    visto: scan shell output for linker errors, parse pty filter tokens
    extra: tick binary symbols panel, has value

entre abiertas:
  T1=>T2  mismo objeto: scan shell output for linker errors
hacia el resto:
  T1 → tick binary symbols panel
  T1 → has value
  T2 → tick binary symbols panel
  T2 → has value
