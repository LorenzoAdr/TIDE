The user wants to investigate how to cleanly cancel AI code generation when the user presses Escape or clicks outside, ensuring the file isn't left half-written.

I have a plan with 3 phases:
1. [fallo] A locator: dónde se captura la tecla Escape o el clic fuera para cancelar la generación de la IA
2. [fallo] B locator: dónde se limpia el archivo para evitar que quede a medias al cancelar la generación
3. [pendiente] P puente: hay camino capturar la cancelación y limpiar el archivo (need A B)

Both A and B have failed.
T1 (A) closed: "no encontré el mecanismo de captura de Escape o clic fuera para cancelar la IA; lo leído son funciones de utilidad escape de strings, hover UI y el efecto de cancelación cancel level1, pero no el disparador UI"
T2 (B) closed: "leído: cancel current, clear pending insert, cancel all, handle user input, cancel level1"

The system says: "ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo."

I need to decide what
---- content ----
{"action":"control_v1","do":"cerrar","why":"el pack no une el disparador UI (Escape/clic fuera) con la limpieza del archivo; las cazas fallaron en encontrar el mecanismo de captura y el puente no se puede trazar sin él"}