The user wants to know if the application remembers the cursor position, open files, and sidebar visibility when reopening a previously opened project.

I have three completed jobs:
1.  **T1 (Save/Restore State):** Looked for general state saving/restoring. Found `save state`, `serialize ai settings`, `serialize ui colors`. The closed report says "ESA pregunta no se relanza...". It implies the mechanism found wasn't the trigger for the specific user request (restoring *exact* state like cursor/files). It found serialization of settings/colors, but not the specific "workspace state" restoration mechanism for files/cursor.
2.  **T2 (Cursor Position):** Looked for cursor restoration. Found `open file at impl` calls `buffer reset to single cursor` which applies position with `cursors front`. The closed report says it found the mechanism for restoring position *when opening a file* (explicit coordinates), but notes it didn't read the automatic restoration from history if opening without coordinates. However, it anchors the "restoration on open" object to these functions.
3.  **T3 (Open Files):** Looked for restoring open files. Found `load` which restores recent project paths, but *not
---- content ----
{
  "action": "control_v1",
  "do": "cerrar",
  "why": "el mecanismo de restauración de posición del cursor al abrir un archivo está confirmado (open file at impl llama a buffer reset to single cursor que aplica la posición con cursors front). sin embargo, no se encontró el mecanismo para restaurar automáticamente la lista de archivos abiertos ni el estado de los paneles laterales al volver a abrir un proyecto (load solo restaura rutas recientes). el claim de recordar 'exactamente' todo el estado se refuta parcialmente: se confirma la posición del cursor en la apertura, pero se refuta la restauración de archivos y paneles."
}