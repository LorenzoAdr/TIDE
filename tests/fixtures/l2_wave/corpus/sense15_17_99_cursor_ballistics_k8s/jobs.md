### Trabajo 1
consulta: dónde se calcula la trayectoria balística de los cursores del editor para sincronizarlos en tiempo real con un cluster de Kubernetes cuando el usuario parpadea
keep: M1 M9
leído: (nada)
Cerrado:
El hijo cerró esta pregunta: no hay objeto. Está contestada. No se relanza.
Preguntó: dónde se calcula la trayectoria balística de los cursores del editor para sincronizarlos en tiempo real con un cluster de Kubernetes cuando el usuario parpadea
ESA pregunta no se relanza. Cierra ESE mapeo, o un objeto distinto del pack (un extra del Abierto); no el mismo claim en otro keep; no refutes el claim entero.
no encontré el cálculo de trayectoria balística ni sincronización con Kubernetes; lo leído es un editor de texto local editor panel con lógica de scroll y breadcrumbs, sin infraestructura de cluster ni detección de parpadeo
Abierto:
- cursor trajectory
- kubernetes sync
- blink detection

### Trabajo 2
consulta: dónde se persiste la física en el índice vectorial cuántico del workspace
keep: M5 M8
leído: symbol workspace indexer cpp, sync symbol workspace indexer
Cerrado:
leído: symbol workspace indexer cpp, sync symbol workspace indexer
Abierto:
- start scan


# control_opened_v1
n=2  (visto+hops extra+circuito; sin código)

T1
    visto: (nada)

T2
    visto: symbol workspace indexer cpp, sync symbol workspace indexer
    extra: request ai indexes, snapshot

entre abiertas:
  T1=>T2  sin camino
hacia el resto:
  T2 → request ai indexes
  T2 → snapshot
