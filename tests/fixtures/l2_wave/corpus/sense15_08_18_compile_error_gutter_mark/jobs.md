### Trabajo 1
consulta: dónde se marca la línea roja en el margen del editor para resaltar un error
keep: M1 M10
leído: schedule visual highlight debounce wake, mark debounce wake scheduled, line gutter marker, fold gutter marker, line diagnostic marker from map, diagnostics for editor line
Cerrado:
ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo. Si el ancla pedía más, un objeto distinto del pack; no el mismo claim en otro keep; no refutes el claim entero.
no encontré el mecanismo que marca la línea roja visual highlight en el margen; lo leído es la lógica que decide qué carácter mostrar para error en el gutter, pero no cómo se pinta o resalta visualmente la línea completa o el margen con color rojo. El sistema parece usar caracteres ASCII en el gutter para indicar errores, no una línea roja continua.
Abierto:
- handle fold gutter click
- paint gutter
- draw diagnostic line


# control_opened_v1
n=1  (visto+hops extra+circuito; sin código)

T1
    visto: schedule visual highlight debounce wake, mark debounce wake scheduled, line gutter marker, fold gutter marker
    extra: Make Editor Panel, tick visual highlight scheduler
hacia el resto:
  T1 → Make Editor Panel
  T1 → tick visual highlight scheduler
