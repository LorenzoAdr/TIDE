### Trabajo 1
consulta: dónde se calcula la trayectoria balística de los cursores del editor para sincronizarlos en tiempo real con un cluster de Kubernetes cuando el usuario parpadea
keep: M1 M10
leído: clamp cursors for folds, ensure scroll visible fold aware
Cerrado:
ESA pregunta no se relanza. Lo leído no era el disparo. PROHIBIDO el mismo claim. do=cerrar ese mapeo. Si el ancla pedía más, un objeto distinto del pack; no el mismo claim en otro keep; no refutes el claim entero.
no encontré el mecanismo de trayectoria balística ni sincronización con Kubernetes; lo leído es lógica local de ajuste de cursores por folds y parpadeo de UI
Abierto:
- handle fold gutter click
- trayectoria balística
- sincronización Kubernetes


# control_opened_v1
n=1  (visto+hops extra+circuito; sin código)

T1
    visto: clamp cursors for folds, ensure scroll visible fold aware
    extra: handle fold gutter click, handle editor mouse
hacia el resto:
  T1 → handle fold gutter click
  T1 → handle editor mouse
