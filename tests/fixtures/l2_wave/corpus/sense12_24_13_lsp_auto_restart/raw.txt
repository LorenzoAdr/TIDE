The user wants to implement automatic detection and restart of the LSP server if it crashes or becomes unresponsive.
I have two completed jobs:
T1: Looked for detection of LSP crash/unresponsiveness. Found `send request`, `wait response`, `write request`.
T2: Looked for restarting LSP after crash. Found `restart lsp for workspace`, `send request`, `wait response`.
T2's closed note says: "no encontré el mecanismo de detección de caída del servidor LSP; lo leído es el efecto del reinicio restart lsp for workspace y la capa de transporte
---- content ----
{
  "action": "control_v1",
  "do": "explorar",
  "consulta": "dónde se detecta que el servidor de lenguaje LSP se ha caído o dejado de responder",
  "hacia": [
    "wait response"
  ],
  "why": "T1 y T2 no hallaron el disparo; el pack apunta a wait response como capa de transporte que retorna false en timeout error; un hijo caza el observador de ese fallo"
}