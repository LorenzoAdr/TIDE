The user wants to mark compilation errors from the bottom console with a red line in the left margin of the editor at the exact line of the error.

Current state:
- T1 found: `line gutter marker`, `diagnostics on line`, `line diagnostic marker from map`, `Make Editor Panel`. This is the UI mechanism for drawing the red line.
- T2 asked: where are compilation errors extracted from the console text. Result: "no object". This mapping is closed.
- T3 asked: where is the console output text read to process it. Result: `set inferior pid`, `source panel contains mouse`, `on pty output`.

The pack shows:
- T1 => T2 sin camino
- T1 => T3 sin camino
- T2 => T3 sin camino

T2 failed to find an object. T3 found `on pty output` etc., but T1 and T3 have "sin camino" (no path) between them.
The anchor claims: "errors appearing in the bottom console should ALSO be marked with a red line...".
We have the UI mechanism (T1: `line gutter marker`, `diagnostics on line`).
We have the console
---- content ----
{
  "action": "control_v1",
  "do": "cerrar",
  "why": "el mecanismo de la línea roja existe (line gutter marker, diagnostics on line), pero no hay camino entre la lectura de la consola y el editor; el pack cierra sin camino"
}