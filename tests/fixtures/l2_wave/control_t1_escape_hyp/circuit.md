# control_opened_v1
n=1  (visto+hops extra+circuito; sin código)

T1
    visto: ai controller hpp, cancel current, handle user input, is cancel input
    extra: handle route, clear pending insert
hacia el resto:
  T1 → handle route
  T1 → clear pending insert
