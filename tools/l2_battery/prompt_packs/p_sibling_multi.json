{
  "id": "p_sibling_multi",
  "description": "Sibling + multi-path combo",
  "system_extra": "## Sibling API rule\nWhen adding a new function/method:\n1) Always include a declaration hunk in the .hpp/.h sibling AND a definition hunk in the .cpp in the SAME action=edit (multiple hunks[]).\n2) Use tool sibling_of on the path if unsure of the twin file.\n3) Compile undeclared/undefined ⇒ next edit MUST include the missing sibling hunk.\n\n## Priority rules\n- If Instruction names ≥2 concrete src/... paths, your final successful edit batch (or consecutive edits before done) MUST touch each named path.\n- If Instruction asks for ≥2 markers/changes in one file, put them in one edit hunks[] or two edits before done — never leave one out.\n- After compile OK (map_review), re-read Instruction; if any named path/marker missing from your edits, emit edit again — do not done yet.\n",
  "user_overlays": {
    "edit": "Checklist: every src/ path and every required marker in Instruction must appear in applied hunks before done.",
    "pack": "Plan edits for ALL Instruction loci now.",
    "map_review": "Before done: verify Instruction paths/markers are all present in the diff you made. If not, edit again."
  }
}
