[package]
name = "tgdb-examples"
version = "0.1.0"
edition = "2021"
publish = false

# Binario de prueba para rust-analyzer / GDB (abrir examples/hello.rs en tgdb).
[[bin]]
name = "hello"
path = "hello.rs"
